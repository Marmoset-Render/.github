# .github
Marmoset Render keeps scenes you own in one named lookdev desk so lights stay easy to reverse on a host you already operate. Marmoset Toolbag keeps the baker honest, so Marmoset Toolbag 5 stays a file you can still restore later without extra noise on that TBSCENE.
